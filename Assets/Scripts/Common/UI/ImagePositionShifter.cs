using System.Collections;
using UnityEngine;

namespace Match3Bonus
{
    public class ImagePositionShifter : MonoBehaviour
    {
        /*private float positionOffse

        [SerializeField] private bool _isRepeating;

        public void StartShiftRoutine()
        {

        }

        private IEnumerator MoveAnimation()
        {
            isAnimating = true;

            Vector3 targetPosition = originalPosition + new Vector3(0f, 1f, 0f); // Example: Move up by 1 unit
            float t = 0f;

            while (t < moveDuration)
            {
                t += Time.deltaTime;
                float normalizedTime = Mathf.Clamp01(t / moveDuration);
                highlightImage.position = Vector3.Lerp(originalPosition, targetPosition, normalizedTime);
                yield return null;
            }

            highlightImage.position = originalPosition;
            isAnimating = false;
        }*/
    }
}
